# Data folder

To resolve the issues with the volumes, instead a mount would be used. To ensure no conflicts 
occur between the volume and the mount of each container (QGIS and QGISServer), the data folder was
moved.
